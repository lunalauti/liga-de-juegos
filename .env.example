# --- API (apps/api) ---
PORT=3001
ALLOWED_ORIGINS=http://localhost:5173
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres
SUPABASE_URL=https://[REF].supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
CRON_SECRET=
# T10.2 — generar una sola vez con: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL= # opcional, mailto: de contacto técnico que pide el estándar VAPID

# --- Web (apps/web) ---
VITE_API_URL=http://localhost:3001
VITE_SUPABASE_URL=https://[REF].supabase.co
VITE_SUPABASE_ANON_KEY=
# Misma clave pública que VAPID_PUBLIC_KEY de arriba — es pública por diseño
VITE_VAPID_PUBLIC_KEY=
